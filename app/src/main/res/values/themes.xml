<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Universe" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Universe" parent="Base.Theme.Universe" />

    <style name="Theme.Universe.Alternative" parent="Base.Theme.Universe">

        <item name="colorPrimary" type="color">@color/universe_light_primary</item>
        <item name="colorPrimaryContainer" type="color">@color/universe_light_primary_container
        </item>
        <item name="colorSecondary" type="color">@color/universe_light_secondary</item>
        <item name="colorSecondaryContainer" type="color">
            @color/universe_light_secondary_container
        </item>
        <item name="colorTertiary" type="color">@color/universe_light_tertiary</item>
        <item name="colorTertiaryContainer" type="color">@color/universe_light_tertiary_container
        </item>

    </style>

    <style name="chipStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

</resources>